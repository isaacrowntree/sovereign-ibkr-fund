{
  "name": "ibkr-fund-watchdog",
  "version": "0.1.0",
  "private": true,
  "description": "Pi-side liveness watchdog for the bezant container: restarts only a dead gateway, reconnects a silent event stream, never restarts in quiet hours unless dead.",
  "type": "module",
  "main": "index.ts",
  "scripts": {
    "start": "tsx index.ts"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "tsx": "^4.19.2"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "typescript": "^5.7.2"
  }
}
