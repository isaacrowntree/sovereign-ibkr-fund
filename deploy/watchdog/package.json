{
  "name": "ibkr-fund-watchdog",
  "version": "0.1.0",
  "private": true,
  "description": "Pi-side liveness watchdog. Restarts the bezant container when /health goes 5xx for too long, or when bezant-relogin self-disables after 3 consecutive auth failures.",
  "type": "module",
  "main": "index.ts",
  "scripts": {
    "start": "tsx index.ts"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "tsx": "^4.19.2"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "typescript": "^5.7.2"
  }
}
