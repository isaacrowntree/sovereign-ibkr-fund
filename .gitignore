node_modules/
dist/
.env
.env.local
*.log
.DS_Store

# Local state (never published) — keep state OUTSIDE the checkout via STATE_DIR
bot-state.json
bot-state.db
bot-state.db-*
trade-history.json
*.db
*.migrated
*.bak-presqlite

# Private portfolio override (real book) — public repo ships only sample.ts
src/portfolios/local.ts

# Private research notes. Same reason as local.ts: this is a PUBLIC repo, and
# anything discussing the real book's holdings, weights, NAV or performance
# belongs beside the code but must never be published with it. Committed
# analysis in docs/ is methodology only, with no portfolio figures.
docs/private/

# Backtest data
src/validation/data/

# DB dumps — may contain agent credentials; never commit
backups/

# host marker: this checkout runs prebuilt artefacts, never builds. Must NOT be
# committed — it would make every clone refuse to build.
.prebuilt
